import { useSelector } from "../../store";
import TeamDetail from "./TeamDetail"

const TeamSquad: React.FC = () => {
    // const squad = useSelector((state) => state.)
    return (
        <TeamDetail>
            <div>팀스쿼드</div>
        </TeamDetail>
    )
}

export default TeamSquad;